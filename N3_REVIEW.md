# N3 审阅记录

日期：2026-09-09

状态：实现完成，等待制作人审阅；不得进入 N4。

## 可玩内容

- 史莱姆会以 2 格/秒追击，接触蛇头扣 1 心并反弹，蛇身会阻挡它；14 HP，击败掉落 2 颗落地豆。
- 蘑菇使用四帧 `mushroom` 美术，但保持针刺花行为：固定不移动、16 HP、4 秒射击周期、提前 0.7 秒显示预警环、针刺速度 2 格/秒。
- 玩家为 3 心，受伤后有 1 秒无敌。针刺碰墙销毁，碰蛇身反射，碰蛇头扣 1 心。
- 敌人与敌方针刺已调整到蛇身/豆子的视觉量级；怪物头部接触会让整条蛇短促闪白并触发 3 像素、0.12 秒的轻微镜头震动。墙撞/自撞的 0.5 秒救援窗改为整条蛇红色脉冲。
- 可蒂占位 NPC 使用 0.85 格进入、1.25 格重新武装的接触迟滞；停留不重复触发，离开再进入可再次触发。互动开始清空输入队列，结束恢复进入前方向。
- 普通监狱为进入 10 + 10 DPS；节点监狱为进入 15 + 30 DPS。每多一个活动节点监狱，节点监狱 DPS 降低 1；离开再进入 burst 有 1 秒冷却。被困敌人每秒最多啃咬一个相邻节点 1 HP。
- 豆、药水对敌人/NPC 走真实 `Area2D` 碰撞，不通过测试专用直调替代运行路径。

## 架构边界

- 一个 `EnemyActor` 类型表覆盖两个当前敌人；第二套真正不同的敌人行为出现前不建立继承树或行为树。
- `EnemyProjectile`、`NpcActor`、`PrisonController` 分别拥有弹体、接触互动、监狱时序状态；现有 `EnclosureDetector` 仍保持无状态。
- 未加入 N4 的 TileMapLayer、机关、Session、检查点和存档，也未提前创建 N7 哥布林骨架。
- 素材只复制本阶段实际使用的两个待机图和三个音效；没有新增许可证清单或发布阻断规则。

## 自动验证

使用 Godot 4.7.2：

```powershell
godot --headless --path . --editor --quit
godot --headless --path . --script res://tests/run_tests.gd
```

结果：`N3 TESTS PASSED (INCLUDING N1/N2 REGRESSION)`。覆盖项目契约、N1 长蛇压力、N2 真实输入与资源循环，以及 N3 追击/接触、蘑菇预警和首发、敌弹头身分流、豆/药水 Area2D、NPC 迟滞/方向恢复、普通与节点监狱的 burst/DPS/重入/啃咬，以及敌人释放后 HUD 的生命周期回归。测试退出无对象或资源泄漏告警；本机仅保留无权限写 `user://` 日志和读取系统证书的环境提示。

图形渲染截图：`.godot/n3_test_arena.png`，确认敌人/NPC/HUD/预警环在 Compatibility 渲染器中可见。`.godot` 属于本机验收产物，不提交仓库。

## 审阅建议

1. 先体验史莱姆的追击、蛇身卡位与头部接触反弹。
2. 观察蘑菇黄色预警环到针刺发射的节奏，以及针刺撞身体后的反射。
3. 用豆攻击两个敌人，确认血条和掉豆；接近可蒂，确认停留不刷屏。
4. 用身体和节点围住目标，观察 HUD 的活动监狱计数与伤害反馈。

已知边界：NPC 和敌方弹体目前是可辨识的代码绘制占位表现；正式对话框在 N5，正式可蒂造型在 N6，统一像素打磨在 N8。
