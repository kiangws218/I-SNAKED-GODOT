# UI / HUD 阶段审阅

状态：等待制作人试玩审阅。

## 本阶段完成

- 开始菜单已包含开始游戏、加载游戏、设置和退出游戏；开始/加载进入三槽子界面，损坏槽可删除，已有槽不会被“开始游戏”无提示覆盖。
- 暂停菜单已包含继续、保存、加载、设置、返回主菜单和退出；页面在 `SceneTree.paused` 时仍能接收输入，不改变对白/演出的 reason 集合语义。
- 音乐和音效分别使用 `Music`、`SFX` 总线。两个滑杆范围 0%–100%、首次默认 100%，保存到 `user://settings.cfg`，不污染剧情存档。
- HUD 左上以 Heart 图标显示生命；其下只在任务已接取且尚未找到阿见时显示“寻找阿见”。
- 左下胃袋以豆子为永久默认槽，当前项居中放大、相邻项缩小；Q/E 更新既有库存索引并触发约 0.2 秒可打断横移 Tween。
- 所有当前可吞物都有图标：豆、铁剑、药水、六名角色、可蒂尸体和角色遗骨。
- 完整页面和组件均为 `.tscn`；控制脚本只负责数据绑定、页面信号和动画。

## 人工编辑入口

- 总体 UI 皮肤：`game/ui/fantasy_ui_theme.tres`
- 开始菜单：`game/ui/main_menu_screen.tscn`
- 暂停菜单：`game/ui/pause_menu_screen.tscn`
- 槽位界面与单行：`game/ui/save_slot_panel.tscn`、`save_slot_row.tscn`
- 设置页：`game/ui/settings_panel.tscn`
- HUD 总装：`game/ui/game_hud.tscn`
- 生命、任务、胃袋：`health_display.tscn`、`quest_display.tscn`、`inventory_carousel.tscn`

修改锚点、间距、字号、控件大小和节点位置应优先在上述场景中完成。按钮行为和数据状态才修改对应脚本。

## 自动验证

执行：

```powershell
& 'D:\Godot_v4.7.2-stable_win64.exe\Godot_v4.7.2-stable_win64_console.exe' --headless --path . --script res://tests/run_tests.gd
```

预期结果：`UI/HUD TESTS PASSED (INCLUDING N1-N7 REGRESSION)`。

新增门禁覆盖双音频总线、0% 静音、音量持久化、四个开始菜单入口、六个暂停菜单入口、三槽渲染、心形状态、寻找阿见任务、胃袋主副位与可打断动画，以及 768×480 原生视口不越界。

## 制作人试玩清单

1. 启动游戏，检查开始、加载、设置、退出四个入口及三个槽位；已有槽需先删除才可重新开始。
2. 分别将音乐、音效拖到 0%、中间和 100%，重启后确认数值恢复；当前尚无背景音乐，所以音乐滑杆只验证保存和总线。
3. 游戏中暂停，依次检查保存、加载、设置、返回主菜单和继续；确认暂停时世界不移动。
4. 受伤与治疗后检查心形数量；未接任务时不应显示普通剧情目标。
5. 接取“寻找阿见”后确认任务出现，找到阿见后消失。
6. 吞入铁剑、药水或角色，连续按 Q/E，检查主位放大、相邻项缩小、横移动画不抖动且吐出物与选择一致。
7. 在 768×480 与更大窗口下检查菜单居中、文字不截断、HUD 不遮挡右侧对话框。

## 已知边界

- 当前没有背景音乐素材；`Music` 总线已准备好，但不会伪造无来源音乐。
- 退出游戏在桌面构建调用 `SceneTree.quit()`；浏览器平台是否允许关闭标签页留到 N8 Web 构建实测。
- Headless 环境无法生成可靠像素截图（dummy renderer），因此自动门禁验证控件全局矩形；颜色、字号和动画手感仍需制作人实际窗口试玩。
