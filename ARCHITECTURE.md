# I SNAKED-GODOT 最小架构

状态：N7 审阅版（待用户审阅）

引擎：Godot 4.7.2

范围：单人剧情模式

## 1. 边界

- 新项目独立存在，不复制旧 Godot 工程。
- 网页剧情模式说明“发生什么”，Godot 决定“怎样实现”。
- 场景表达可摆放对象，脚本表达行为，Resource 只表达多个实例共享的静态定义。
- 每类状态只有一个写入口；signal 广播已发生的事实，不充当命令总线。
- 常规地图墙体的视觉和碰撞来自同一个 TileSet tile，避免维护第二张隐形墙图。

## 2. 首个可玩切片的运行树

```text
Game
├── Session
├── PauseCoordinator
├── WorldHost
│   └── CurrentMap
└── UILayer
    ├── HUD
    ├── DialogueBalloon
    ├── PauseMenu
    └── Transition
```

- `Session` 拥有当前会话数据、地图切换和检查点。
- `PauseCoordinator` 独占 `SceneTree.paused` 写权限，用 reason 集合处理对话和菜单叠加。
- `WorldHost` 换图时销毁旧地图后再实例化新地图。
- UI 不放在地图下面，换图不会销毁对话和 HUD。

N1 出现这些节点前不创建对应脚本。

## 3. 状态边界

```text
SessionState
├── player      身长、生命、方向、节点充能
├── inventory   胃袋槽位、数量、选中项
├── story       章节、任务、flag、角色剧情身份
└── world       当前地图、机关、遭遇、地面剧情物品
```

运行时节点是表现，不是第二份持久事实。共享 `.tres` 视为只读，当前生命和数量不写回定义资源。SaveService 未来只序列化纯数据，不拥有游戏状态。

## 4. 玩家与物理

- 蛇头使用 `CharacterBody2D`。
- `BodyChain` 的连续轨迹是身体位置唯一权威；渲染、占用格、自撞和碰撞代理全部由它派生。
- 不默认“一节一个完整物理节点”。N1 用目标蛇长/弹体压力场决定代理方案和预算。
- 头部是受伤弱点；身体负责自撞、弹体折射、围圈和实体阻挡。
- NPC/机关交互使用 `Area2D`；普通阻挡使用 TileSet 物理层或 `StaticBody2D`。
- 自主移动的玩家/敌人使用 `CharacterBody2D`；只做 overlap 的豆子可使用 `Area2D`。

## 5. 剧情与存档

- 计划在需要对白的阶段固定并接入 Dialogue Manager；N0 不复制插件。
- `story_graph.json` 负责台词、选项、条件和跳转；不直接生成敌人、切地图、扣物品或改碰撞。
- 游戏命令通过白名单 StoryBridge 执行：完整校验 → 单次提交 → 发出结果。
- 一次性事件使用稳定 ID 防重放；换图/重试提升 session generation，旧 signal 不得影响新会话。
- 只在对话关闭、换图完成、检查点激活和无待提交命令的显式保存时写盘。
- 不保存 Node、Tween、Timer、协程、signal 连接或对话等待句柄。

## 6. 阶段

### N0：独立项目与事实基线（已完成）

- 独立工程、占位主场景、headless 测试入口。
- 新 `GDD.md`、`CONTENT_SCOPE.md`、架构边界、团队交接规则。
- 不复制插件、素材或旧代码。

出口：文件结构与内容追踪矩阵可审查；Godot/GDA 可用时全部验证通过。制作人确认后进入 N1。

### N1：蛇头、连续移动与轨迹身体

- TestArena、一堵墙、连续八方向、禁止 180°、2 倍加速。
- 身体轨迹、占用格、自撞和 0.5 秒救援窗。
- 输入绑定和最大目标蛇长压力基线。

出口：真实输入可连续游玩，墙/自撞行为与网页剧情模式一致，性能方案可接受。

### N2：豆子、胃袋与节点

- 站桩吐豆、反弹/减速/回收、身长弹药。
- 胃袋、承重、Q/E、特殊投射物、断尾。
- 固定节点、穿越豁免、回收、围圈检测。

出口：核心资源循环在 TestArena 完整闭环。

实现边界：`SnakePlayer` 直接拥有一个 `StomachInventory` 纯数据对象并生成世界实例；统一 `BeanProjectile` 承载豆、铁剑、药水和角色载荷；`RingNode` 只保存格子、HP 与生命周期；`EnclosureDetector` 是无状态四邻接洪泛。没有新增全局管理器、事件总线、通用战斗接口或逐节身体物理节点。

### N3：敌人、NPC 与监狱

- 一个近战敌人、一个远程敌人、一个 NPC。
- 远程敌人采用现有 `mushroom` 美术，但行为与数值保持针刺花基线；美术名不能反向决定战斗逻辑类名。
- 头部受伤、身体折射/阻挡、监狱瞬时与持续伤害。
- NPC 自动接触交互的去重和对话前方向恢复。

出口：历史碰撞、重叠和重复互动问题进入回归测试。

实现边界：`EnemyActor` 用一个类型表承载史莱姆与蘑菇的少量静态差异；`EnemyProjectile` 独立处理针刺的墙体命中、身体反射与头部伤害；`NpcActor` 只负责接触迟滞和互动方向快照；`PrisonController` 在现有 `EnclosureDetector` 结果上维护 burst、DPS、重入冷却和节点啃咬状态。没有加入行为树、通用属性组件、全局战斗管理器或 N4 机关接口。

### N4：地图、机关、会话与存档

- 四张剧情地图的最小 TileMapLayer 实现。
- 门、出口、桥桩、地图切换、检查点、死亡重试、三槽存档。

出口：无对白也能从教学地图走到第一章洞窟并可靠读档。

实现边界：四张地图各自保存为 `game/maps/levels/*.tscn`，场景内 `Ground` 与 `Collision` 是可在 Godot TileMap 面板直接绘制的 `TileMapLayer`，共享外部 `story_tileset.tres`。`StoryMapCatalog` 只保存出生点、出口、机关和内容生成点等玩法元数据；`StoryMap` 实例化地图场景，不生成地形。`GameSession` 是正式游戏根节点和换图/检查点所有者，不是测试切片；`test_arena.tscn` 才是独立测试场。

### N5：剧情基础设施

- `story_graph.json` 是单一剧情图源，`DialogueRunner` 只负责分页、条件、变量与选择，`StoryDirector` 负责真实玩法事件和安全命令；不引入 Dialogue Manager 插件。
- 对话正文、剧情变量和 UI 表现分离；对话框用单一表现状态机管理可接管的弹入/弹出 Tween。
- 弹入中的确认只完成动画，不能推进文本；弹出时拒绝推进，世界暂停与 UI 动画各自使用独立处理域。
- 138 节点、58 action 名、18 条件、42 flag 的逐 ID manifest 机械校验；N6/N7 命令按阶段门禁执行，未知命令以 `UNKNOWN_COMMAND` 失败。

出口：已完成。序章命令端到端工作，坏命令明确失败，主菜单与暂停 reason 已接入。

### N6：序章垂直切片

- 教学地图、荒野、可蒂事件和对应 UI/占位素材。

出口：已完成。新游戏可经真实吃豆、吐豆、破门、出口、可蒂分支与史莱姆事件进入森林 N7 起点；等待制作人审阅。

### N7：第一章

- 森林与河岸、阿杰/丽丝、铁剑、洞窟、阿见、哥布林、桥桩和营地结算。

N7 已完成第一章状态与剧情接入：第一章 action 白名单可执行，剧情物品通过对白选择互动，角色保持稳定身份并支持吞吐、唤醒、伤亡与骑乘状态，哥布林双实例、桥桩、营地和劫匪结算具备重入保护。N7A 的可视化关卡编排基础继续作为编辑入口：位置由地图场景拥有，五个编辑分组承载角色、机关、拾取物、触发区和出生点；地图换图统一渐黑，NPC 互动镜头统一聚焦/恢复。未引入 EventBus、Service Locator 或全局演出系统。

出口：第一章主要分支、存档和重放已通过 N1–N7 自动回归，等待制作人试玩确认；正式美术、音效和整体打磨留在 N8。

2026-09-12试玩反馈保持原边界：EnemyActor拥有视野脱战计时；NpcActor拥有头触扫掠、成对局部游走和登蛇Tween；StoryDirector只路由战后回触、任务与演出成功提交；SessionState准备一次性回营，StoryMap只捕获真实实体并消费一次性出生锚点。DialogueRunner负责展示分页，不改对白源。新增camp_followup后当前图与manifest为140节点，不增加全局管理器或第二份存档状态。

### UI/HUD：场景化界面完成阶段

- `MenuController` 只协调页面和信号；开始、暂停、槽位、设置均由独立 `.tscn` 持有锚点、尺寸、间距与控件。
- `AudioSettingsStore` 只保存音乐/音效百分比并映射 `Music` / `SFX` 总线，不进入剧情存档。
- `GameHud` 组合 `HealthDisplay`、`QuestDisplay` 与 `InventoryCarousel`；剧情任务只读取既有 `quests.findAjian` 和稳定 flag，不另建任务事实源。
- 胃袋动画只表现 `StomachInventory.selected_index` 的变化，不能反向拥有或修改库存。
- HUD 的生产节点无装饰底框，内部兼容/调试标签保持隐藏；设置键位页只读 InputMap，不另存一份容易失效的按键表。
- 对话数据与表现状态机不变；透明边框、淡化分隔线和颜色集中在 `dialogue_theme.tres`，不侵入剧情解释器。
- UI 继续位于地图之外并使用暂停可处理模式；没有新增 EventBus、Service Locator 或全局演出管理器。

出口：场景化菜单、三槽存读档、音量持久化、正式 HUD 和 768×480 原生视口门禁完成，等待制作人试玩审阅。

### N8：整体打磨与学习构建

制作人单独批准的当前子范围：六名NPC小人与重复可调用的导入管线。NpcActor共用AnimatedSprite2D，配置化开发期导入生成静态资源，运行时不切帧、不调用AI、不持有第二份角色状态。六人移动已验收，制作人批准与可蒂头像、八项试玩优化一起提交推送；全局音效、其他打磨与构建仍未批准。详见NPC_ART_PIPELINE.md与PLAYTEST_FEEDBACK_REVIEW.md。

- 全流程难度、镜头、UI 动效、音效、像素一致性和无障碍提示收尾。
- 压力测试长蛇、多弹体、反复换图/读档和全部剧情分支；清理调试入口与未引用资源。
- 使用 Godot 4.7.2 官方模板生成 Windows 与 Web 学习构建，并做从零启动验收。

出口：序章和第一章形成稳定、可独立运行的个人学习版本；构建产物可启动且不依赖编辑器。

## 7. 完成定义

- 当前阶段范围之外没有新增框架。
- 真实输入、碰撞或 signal 路径通过；不能只调用内部方法假装玩家操作。
- Godot 4.7.2 headless 测试以非零退出码报告失败。
- GDA 可用时脚本全量 validate、相关 scene validate/preflight 均通过，且所有命令使用 `--json`。
- 到达阶段出口后停止并等待制作人审阅。

本项目仅供个人学习。
