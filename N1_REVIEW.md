# N1 阶段审阅单

状态：实现与自动门禁完成，等待制作人审阅

日期：2026-09-08

## 已完成

- Godot 4.7.2 原生 `CharacterBody2D` 蛇头与 TestArena。
- WASD/方向键实际 InputMap 绑定；连续八方向、归一化斜向和禁止 180°。
- 最多 3 个有效方向的 FIFO 缓冲；重复和相对未来方向的精确反向不占队列。
- 4 格/秒基础速度；按住当前方向为 8 格/秒，释放后继续以基础速度前进。
- 单一连续历史轨迹派生身体段与占用格；1 格间距、斜向桥接格和路径裁剪。
- 颈部索引 0–3 豁免，索引 4 起以严格 `<0.62` 格判定自撞。
- 墙撞/自撞暂停玩家移动并进入 0.5 秒救援窗；安全方向优先于边界超时判死。
- 危险提示、死亡提示与 Enter 重开。

## 自动门禁

```powershell
& 'D:\Godot_v4.7.2-stable_win64.exe\Godot_v4.7.2-stable_win64_console.exe' --headless --path . --editor --quit
& 'D:\Godot_v4.7.2-stable_win64.exe\Godot_v4.7.2-stable_win64_console.exe' --headless --path . --script res://tests/run_tests.gd
```

最终结果：`N1 TESTS PASSED`。

性能样本：256 节完整轨迹，连续 120 次重建，平均 `0.539 ms`、P95 `0.730 ms`。门禁预算为平均不超过 `2 ms`、P95 不超过 `4 ms`。

实际 OpenGL Compatibility 录制 768×480、60 FPS、60 帧：CPU 渲染平均 `1.04 ms/帧`，GPU 平均 `1.25 ms/帧`，无运行错误。

## 有意差异与延期

- Godot 使用 `test_move()` 在碰撞前拦截蛇头，不复制网页版轻微进入墙体后再停下的缺陷。
- 冲刺遵循本项目 GDD：只在按住当前方向时生效；不复制网页版“任意方向键按住都加速”。
- 精确反向在入队时过滤，避免无效输入挤掉有效缓冲。
- N1 身体只负责轨迹、占用格和玩家自撞。供敌人/弹体寻址的动态碰撞代理在 N3 按压力数据决定，不提前建立节点池。
- 节点附近的身体自撞豁免在 N2 与节点系统一起验收。

## 制作人手感审阅

运行主场景后检查：连续移动、斜向、快速三次转向、反向拒绝、按住当前方向加速、撞墙救援成功和不救援死亡。物理按键手感属于人工验收；自动测试使用合成 `InputEventKey` 走同一 InputMap 与物理处理路径。
