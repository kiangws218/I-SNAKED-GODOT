# N4 阶段审阅：地图、机关、会话与存档

状态：实现完成，等待制作人审阅；未进入 N5。

## 可玩审阅

- 主场景从 72×48 教学地图启动；四图均使用 Godot 4.7 `TileMapLayer` 与共享外部 `TileSet`。
- F1/F2/F3/F4 是明确标记的阶段审阅跨图入口，依次进入教学、荒野、森林、洞窟；不写入剧情完成事实。
- 教学门承受三枚豆后开启，出口随后可进入荒野。
- 森林桥柱必须被蛇身闭环包围且闭环接触至少一个环形节点；持续 3 秒打开桥面，中断按每秒 0.2 秒回退。
- 1/2/3 选择槽位，F5 保存、F6 载入、F9 重试。死亡会销毁旧地图并从入口检查点独立快照恢复，生命补满。

## 数据与边界

- `StoryMapCatalog` 登记四图尺寸、出生点、出口、地形、障碍、豆、NPC、物品、敌人、遭遇和触发区；森林洞口使用网页版最终覆盖后的中央上方坐标。
- N4 显示地图、出口、门、桥柱和物品位置标记，并提前接通洞窟环形节点的基础拾取，便于无对白验证桥柱。NPC、敌人及其他物品副作用仍归 N6/N7。
- `SessionState` 预留并实际序列化剧情、玩家、胃袋、身体、节点、金币、机关、奖励、遭遇和物品事实。
- `SaveStore` 使用 schema v1 三槽 JSON；覆盖写经临时文件提交，损坏 JSON、未来版本、缺失迁移和无效地图分别返回明确错误。

## 验证证据

Godot 4.7.2：

```powershell
godot --headless --path . --script res://tests/run_tests.gd
```

结果：`N4 TESTS PASSED (INCLUDING N1/N2/N3 REGRESSION)`。

覆盖四图注册、共享 TileSet 物理层、真实 TileMapLayer 单元、三豆门、桥柱充能/回退/开桥、三槽隔离/覆盖/删除、坏 JSON、版本错误、Session 跨图和检查点资源回滚。

实际 OpenGL 3 渲染截图：`.godot/n4_tutorial_gate.png`、`.godot/n4_forest_bridge.png`（缓存证据，不提交）。

环境仅有两条无关业务的沙箱提示：无法写系统 `user://logs`，无法读取 Windows 根证书。测试存档定向写入被忽略的 `res://.godot/n4_test_saves`，正式运行仍使用 `user://saves`。

## 已知边界

- N4 地图图集是本项目原生的最小 24px 审阅版，已经真实用于地表、水、洞窟、山体、营地和 World 碰撞；N8 再统一最终像素细节。
- 尚未接入 N5 对话框，因此荒野到森林使用 F3 调试入口；这一入口会在剧情通路完成后移除。
- GDA 当前不在 PATH，本阶段使用 Godot 4.7.2 headless 与真实 OpenGL 截图完成门禁。

## 制作人审阅修正

- 不透明地表层降到蛇身下方，修复只能看见蛇头。
- 荒野与森林由绿色地表改为暖棕色，避免与绿色蛇体撞色。
- 教学门改为占据边界中的独立门洞，第三豆后碰撞体消失并留下可穿越缺口。
- 桥柱闭环检测由每物理帧全森林扫描，改为 10 Hz、近桥、已有节点时才扫描 17×20 局部区域；远处只进行常数时间回退。
- 换图后立即重置 Camera2D 平滑，消除静止地图豆从画外滑入的错觉；预置豆出生即落地。
- 洞窟环形节点可直接拾取并解锁 1 点节点充能；正式剧情对白仍留在 N7。桥柱充能按制作人反馈改为 3 秒。
- 蛇节视觉连接方案经制作人审阅后撤销，恢复原分节绘制；无缝外观问题登记为待定，后续结合正式蛇身美术统一处理。
