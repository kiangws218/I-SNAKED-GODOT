# N7 审阅：第一章剧情与状态接入

状态：等待制作人试玩审阅

## 本阶段完成

- 第一章 58 个 action 名已从“阶段外延后”切换为真实实体与剧情状态执行；未知 action 仍明确返回 `UNKNOWN_COMMAND`。
- 森林、荒野、洞窟和教学地图沿用场景拥有对象位置的编排方式；地图切换统一渐黑/渐亮，NPC 互动镜头聚焦蛇头与目标后恢复。
- `Actors`、`Interactables`、`Pickups`、`Triggers`、`SpawnPoints` 五类编辑分组保持稳定；剧情物品（铁剑、汤、药水、环形节点）通过对白选择完成互动，环形节点不再自动收集。
- 阿杰、丽丝、阿见和可蒂等剧情角色使用稳定身份；吞入、吐出、唤醒、伤亡、骑乘和跨图恢复不复制角色实例。
- 洞窟哥布林以两个独立 `spawn_id` 实例参与战斗；投射物、落地和再次互动沿真实角色状态流转。
- 森林四只史莱姆与一只蘑菇由可拖拽出生点的 `auto_spawn` 配置生成；死亡、剩余 HP 与位置进入存档，换图和读档不会让已击败敌人复活。
- 桥桩监牢进度、吊桥 `AnimationPlayer` 钩子、营地 10 金结算和劫匪 3/6 金结算均有重入与存档恢复保护。
- 本阶段没有新增 EventBus、Service Locator 或全局演出系统；对白副作用仍由白名单 `StoryDirector` 校验后一次提交。

## 人工编辑入口

1. 在 Godot 4.7.2 打开 `game/maps/levels/prologue_tutorial.tscn`、`wilderness.tscn`、`forest.tscn` 或 `cave.tscn`，在 2D 场景树中编辑对象位置；营地结算位于森林场景。
2. 地图地形在 `Ground` 与 `Collision` `TileMapLayer` 中绘制；视觉和碰撞使用同一 TileSet 数据，不要在剧情脚本中补坐标矩形。
3. 角色、机关、拾取物、触发区和出生点分别放在 `Actors`、`Interactables`、`Pickups`、`Triggers`、`SpawnPoints` 分组下；新增实例先在 Inspector 填稳定 ID，再接入剧情节点。
4. 桥梁表现入口为桥桩节点下的 `AnimationPlayer`，已有桥梁动作名应保持不变（当前检查入口为 `lower`）；动画完成后由本地节点 signal/回调推进机关状态。
5. 稳定 ID 是存档、重放和重入去重键：`npc_id`/`persistent_state_id` 用于角色，`spawn_id` 用于遭遇实例，`item_id`/`pickup_id` 用于剧情物品，`trigger_id`/`pillar_id` 用于触发区和桥桩。移动节点可以改位置，但不要在编辑器中重复或复用同一稳定 ID。

## 自动验证

在项目目录执行：

```powershell
& 'D:\Godot_v4.7.2-stable_win64.exe\Godot_v4.7.2-stable_win64_console.exe' --headless --path . --script res://tests/run_tests.gd
```

当前结果：`N7 TESTS PASSED (INCLUDING N1-N6 REGRESSION)`。该门禁覆盖 action manifest、环形节点对白选择、哥布林双实例、森林常驻敌人生成与死亡恢复、角色唯一身份/投射物、骑乘、桥桩/TileMapLayer/AnimationPlayer、营地与劫匪幂等，以及 `SessionState` 存档/检查点。GDA 不在本机 PATH，本阶段使用 Godot 4.7.2 headless 作为等价验证入口。

## 制作人人工验收清单

- 从序章进入森林，确认地图换图渐黑/渐亮，蛇头与目标在对白开始后位于左侧中央安全区，右侧对白框不遮挡后续演出空间。
- 在洞窟依次检查汤、药水和环形节点：不靠接触自动收集，选择对白后才改变状态；形成监牢时按实际穿越/未穿越路径核对进度条。
- 检查阿见的绑定、唤醒、松绑、救治、吞入/吐出和骑乘；换图、死亡重试、读档后仍是同一角色而非球形占位物。
- 触发两名哥布林战斗，确认两名敌人可分别受击、箭矢正常命中、击败后的豆子可回收，战后对白仍能找到阿见实例。
- 以阿杰/丽丝不同清醒与携带组合完成营地结算，重复触发不重复发放 10 金；在劫匪处分别验收支付 3 金、反抢/战斗及重访不重复扣发 6 金。
- 保存、重载和检查点恢复至少各执行一次，确认角色、节点、机关、库存和剧情 flag 一致。

## 已知风险与阶段边界

- 正式角色造型、哥布林匹配素材、节点四状态美术、桥桩色板、专用豆/监牢音效、镜头和整体 UI 动效仍列入 N8 打磨，不应把当前代码绘制表现当作最终美术。
- 当前审阅重点是剧情状态和可玩闭环；不同分辨率下对白框、镜头安全区和演出节奏仍需制作人实际试玩确认。
- 不新增全局事件总线或全局演出管理器；后续局部动画继续优先放在对应场景节点的 `AnimationPlayer` 与本地 signal 路径。
- headless 环境可能输出 `user://logs/godot.log` 写入失败和根证书读取提示；它们不影响本次测试的最终通过结果，但正式构建前仍应在 N8 环境复核。
