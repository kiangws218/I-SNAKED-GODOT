# N2 阶段审阅

状态：已完成，等待制作人确认后进入 N3。

## 可玩结果

TestArena 已形成 N2 核心资源闭环：身体长度既是普通豆弹药，也是特殊物品占长和断尾下限；豆可发射、随机反弹、减速落地并拾回；成功吐出使用平滑制动，CD 内不会因空按反复停顿；胃袋可循环选择铁剑和药水；节点视觉准确落在放置时的蛇身中心，并在身体完全离格后回收充能；围圈区域由四邻接洪泛实时识别。

测试场为了集中验收，预置 12 节身体、两把铁剑、一瓶药水和三点节点充能。剧情正式开局仍按 GDD 保持 4 节身体、0 节点充能，取得洞窟圆环后才解锁。

## 实现边界

- `StomachInventory` 是玩家拥有的纯数据对象：豆槽永久存在，承重上限 6，特殊物品保留长度和 metadata。
- `BeanProjectile` 统一表示豆、铁剑、药水、角色、骨头和尸体载荷，没有按物品复制多套弹体类。
- `RingNode` 只持有格子、2 HP、回收/摧毁状态，不加入 World 实体碰撞。
- `EnclosureDetector` 只做无状态的格子洪泛，不提前建设 PrisonManager、GridWorld 或伤害框架。
- `BodyChain` 仍是身体位置与占用格的唯一权威；长度变化会立即重建，不保留网页旧版的单帧滞后。

## 自动验证

使用 Godot 4.7.2：

```powershell
godot --headless --path . --editor --quit
godot --headless --path . --script res://tests/run_tests.gd
```

最终结果：`N2 TESTS PASSED (INCLUDING N1 REGRESSION)`，退出码 0，无对象或音频资源泄漏。覆盖内容包括：

- N1 全量移动、碰撞救援和 256 节压力回归；本次修订最终验证均值 0.354 ms、P95 0.371 ms，低于 2/4 ms 门限。
- SceneTree 真实分发 J/E/K/F；J 与空格的 InputMap 绑定均存在。
- 0.42 秒射击间隔、0.12 秒平滑制动、CD 内恢复移动、特殊物品松键门、库存先扣后生成。
- 承重 6 边界、豆槽、铁剑堆叠、角色/骨头身份、可蒂尸体两节占长。
- 断尾保留长度、10 秒冷却、`floor(60%)` 精确散豆、节点充能不变。
- 真实 World 墙体反弹、`±0.22 rad` 随机方向、`0.70–0.86` 随机保速、强阻力/落地阈值、药水碰撞破碎。
- 节点 HP、回收返还、摧毁不返还、自撞豁免和普通/节点围圈分类。

Godot 在受限无头环境中仍会报告无法写 `%APPDATA%` 日志/编辑器缓存和无法读取系统证书；这些是宿主权限噪声，脚本编译、资源导入和测试均成功。GDA 当前不在 PATH，因此按项目规则使用 Godot 4.7.2 headless。

## 视觉与素材验收

已用实际 OpenGL 窗口自动截取 `.godot/n2_test_arena.png` 并人工检查：HUD 无截断，蛇身、飞豆、实际环形节点素材和墙体层级可辨。当前只导入三个 N2 音效和一个节点 PNG；豆、铁剑、药水继续使用清晰的代码临时表现，避免为一颗豆导入整张混合食物候选图集。

## 明确留给 N3

- 敌人/NPC 命中、头部受伤、药水实际治疗目标，以及角色投射过程中的双向伤害。
- 封闭区域内有敌人/NPC/机关且邻接身体时的活动监狱判定与瞬时/持续伤害。
- 敌人每秒啃咬一个节点、投射物 EnemyHurtbox mask，以及落地角色的正式 NPC 表现。

这些内容需要 N3 首次出现的真实生命、敌人和 NPC 使用者，N2 不提前制造一次性通用战斗框架。

## 人工审阅建议

1. 长按 J 连吐普通豆，确认每次成功吐出时平滑减速、CD 内恢复移动，约 0.42 秒后再次吐出；撞墙后的豆应散开并可追上拾回。
2. Q/E 选择铁剑或药水；长按只能吐一件，松开后才可再次吐特殊物品。
3. K 断尾后观察长度降到胃袋保护下限和散落豆数量；冷却结束前不得重复。
4. F 放节点后让身体离开所在格，确认节点消失且充能返还；用节点穿过身体封口时不触发自撞。
5. 用身体围出封闭区并开一个缺口，观察 HUD 的封闭区计数成立与解除。
