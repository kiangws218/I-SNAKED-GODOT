# N7A 审阅：可视化关卡编排基础

状态：等待制作人试玩审阅

## 本阶段完成

- 四张地图统一为 `Actors / Interactables / Pickups / Triggers / SpawnPoints` 可编辑结构。
- 玩家入口、地图出口、全部固定豆、剧情物品、可蒂、第一章 NPC 预摆位、序章门、森林桥桩、剧情区域和敌人刷出点均从代码坐标迁入地图场景。
- `StoryMapCatalog` 只保留地图注册、标题和尺寸；`StoryDirector` 的可蒂战史莱姆改用稳定刷怪点 ID。
- 未激活 NPC 保留编辑器实例并关闭显示、物理和交互，后续剧情通过稳定 `npc_id` 启用，不丢失摆放位置；出口锁定条件可在 Inspector 配置。
- 地图切换加入持久 UI 层全屏渐黑/恢复；洞口和普通出口共用同一可靠换图路径。
- NPC 互动开始时镜头在蛇头与目标之间平滑聚焦并放大；互动结束后恢复跟随。Tween 在剧情暂停时继续运行。
- 充能桩和剧情物品提供本地 `AnimationPlayer` 接点，后续吊桥或特殊物品动画由各自场景制作，不需要全局动画系统。

## N7 组件评估

- 保留：`BodyChain`、`BeanProjectile`、`DialogueRunner`、`StoryDirector`、`SessionState` 的职责当前清晰，不在 N7A 重构。
- 已升级：地图对象位置所有权、出口触发、刷怪点、桥桩、镜头和屏幕转场。这些已经直接阻碍人工关卡制作。
- N7B 进入哥布林实现前复核 `EnemyActor`。第三种行为若继续复制生命、接触伤害或监狱接口，再提取共享 `HealthComponent`/命中组件；不为单个新敌人预建行为树或万能状态机。
- N7 角色状态接入时，以 `npc_id` 为唯一身份，把地图、胃袋、投射物、死亡和骑乘状态统一到 `SessionState`；N7A 的预摆角色暂不冒充剧情完成。

## 验证

- Godot 4.7.2：`--headless --path . --script res://tests/run_tests.gd`
- 结果：`N7A TESTS PASSED (INCLUDING N1-N6 REGRESSION)`。
- 森林桥桩空闲压力：600 次 tick 约 100–132 ms，低于 250 ms 门槛。
- `git diff --check` 通过。
- GDA 本机当前不在 PATH，因此按项目规则使用 Godot 4.7.2 headless 门禁；编辑器扫描和主场景启动未发现项目脚本/场景错误。沙箱内 Godot 无法写用户级编辑器缓存和系统证书，这是环境噪声，不是项目错误。

## 制作人试玩重点

1. 在 Godot 打开四张地图，确认上述五个分组和对象都可选中、拖动、保存。
2. 从序章正常换图，观察全屏渐黑是否自然。
3. 接触可蒂，观察镜头是否平滑框住蛇头与可蒂；完成对话后是否平滑恢复。
4. 修改一个豆、可蒂或出口的位置后运行，确认运行时采用场景中的新位置。

已知边界：N7A 只提供作者工作流和表现接口；第一章 NPC、哥布林、吊桥正式美术与剧情命令仍属于后续 N7 子阶段。
