# AGENTS.md

## 权限与目标

制作人是第一负责人；当前主 Codex agent 是第二负责人，负责技术决策、阶段拆分、质量门禁和子 agent 协调。子 agent 只能做主 agent 分配的有界任务，最终取舍与合并由主 agent负责。

项目目标是用 Godot 4.7.2 原生方式重做《我蛇了》的剧情模式。只做剧情项目，不顺手恢复沙盒、关卡战役、肉鸽、Boss 挑战、本地双人或移动端。

## 事实优先级

1. 制作人的最新明确决定。
2. 本项目 `GDD.md`。
3. 旧 `IM-SNAKE/prototype/` 剧情模式的当前可观察行为和源码。
4. 旧项目的源码事实清单与历史反馈。
5. 旧 `IM-SNAKE/GDD.md` 仅作历史背景，不作为本项目数值来源。
6. 旧 `IM-SNAKE/godot/` 只用于识别失败经验，不复制其架构。

## 开发规则

- 每次只推进 `ARCHITECTURE.md` 中的一个阶段；达到出口后停止开发，提交变更摘要、验证证据和已知风险，请制作人审阅。
- 制作人确认前不进入下一阶段。
- 不预建空目录、万能系统、单实现接口、EventBus 或 Service Locator；第二个真实使用者出现后再抽象。
- 旧 `IM-SNAKE` 全部只读。内容或行为要迁移时，先写成当前项目的测试/验收条件，再原生实现。
- `.dialogue` 最终是唯一对白正文源；剧情事实由游戏状态持有，对话工具不能成为第二份存档。
- 剧情副作用必须校验后一次提交，并能防止读档/重入导致重复奖励、重复扣除或重复生成。
- 修改脚本后跑全量脚本校验；修改场景后跑静态校验和 preflight；修改交互后用真实输入验证。
- GDA 可用时每条命令都传 `--json`；不可用时使用 Godot 4.7.2 headless 和 TestArena。
- 不用 PowerShell/正则批量改写中文内容。文件修改使用精确补丁。

## 子 agent 规则

- 最多 4 个 agent 同时工作（包含主 agent），子 agent 统一使用 Luna 模型。
- 并行任务必须边界清晰，避免多人同时编辑同一文件。
- 子 agent 的报告不是项目事实，主 agent 必须回到源码核对后再写入 GDD 或代码。

## 当前阶段

N1 实现与自动门禁已完成，等待制作人审阅。制作人确认前禁止进入 N2。
