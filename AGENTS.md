# AGENTS.md

## 权限与目标

制作人是第一负责人；办公室与家庭两套 Codex 团队通过 `HANDOFF.md` 接力。当值主 Codex agent 是第二负责人，负责技术决策、阶段拆分、质量门禁和子 agent 协调。子 agent 只能做主 agent 分配的有界任务，最终取舍与合并由当值主 agent 负责。

项目目标是用 Godot 4.7.2 原生方式重做《我蛇了》的剧情模式。只做剧情项目，不顺手恢复沙盒、关卡战役、肉鸽、Boss 挑战、本地双人或移动端。

## 事实优先级

1. 制作人的最新明确决定。
2. 本项目 `GDD.md`。
3. 旧 `IM-SNAKE/prototype/` 剧情模式的当前可观察行为和源码。
4. 旧项目的源码事实清单与历史反馈。
5. 旧 `IM-SNAKE/GDD.md` 仅作历史背景，不作为本项目数值来源。
6. 旧 `IM-SNAKE/godot/` 只用于识别失败经验，不复制其架构。

## 开发规则

- 开始工作前先阅读 `HANDOFF.md` 并确认接力棒、远端版本和阶段停止线；新负责人第一次接力还要完成 `LEAD_ONBOARDING.md` 的上岗基线。任一时刻只有一个当值团队可以写入和推送主线。
- 每次只推进 `ARCHITECTURE.md` 中的一个阶段；达到出口后停止开发，提交变更摘要、验证证据和已知风险，请制作人审阅。
- 制作人确认前不进入下一阶段。
- 不预建空目录、万能系统、单实现接口、EventBus 或 Service Locator；第二个真实使用者出现后再抽象。
- 旧 `IM-SNAKE` 全部只读。内容或行为要迁移时，先写成当前项目的测试/验收条件，再原生实现。
- `.dialogue` 最终是唯一对白正文源；剧情事实由游戏状态持有，对话工具不能成为第二份存档。
- 剧情副作用必须校验后一次提交，并能防止读档/重入导致重复奖励、重复扣除或重复生成。
- 修改脚本后跑全量脚本校验；修改场景后跑静态校验和 preflight；修改交互后用真实输入验证。
- GDA 可用时每条命令都传 `--json`；不可用时使用 Godot 4.7.2 headless 和 TestArena。
- 不用 PowerShell/正则批量改写中文内容。文件修改使用精确补丁。

## 子 agent 规则

- 子 agent 统一使用 Luna 模型。团队规模由主 agent 根据阶段依赖、任务边界和协作收益动态决定，不预设固定人数，也不为占位创建空闲成员。
- 实际并发不得超过当值协作环境的硬上限；不同电脑或账号容量不同时，按实际可用容量和任务需要调度。
- 并行任务必须边界清晰，避免多人同时编辑同一文件。
- 子 agent 的报告不是项目事实，主 agent 必须回到源码核对后再写入 GDD 或代码。

### 半固定角色与默认能力域

- 「玩法与手感」：玩家控制、战斗、敌人行为、数值手感和旧网页版玩法事实。
- 「测试与缺陷」：测试设计、真实输入验证、回归、边界条件和定向 bug 审计。
- 「场景与表现」：Godot 场景装配、美术与音效接入、动画、HUD 和视觉检查。
- 「剧情与状态」：剧情流程、NPC、对话、条件分支、剧情副作用和存读档一致性。
- 「地图与遭遇」：TileMapLayer、碰撞/导航、关卡布局、敌人配置和剧情触发区域。
- 「工程与发布」：性能采样、静态检查、构建/导出、工具链、仓库健康和阶段门禁。

以上是能力域，不是固定岗位或固定人数。主 agent 可以把相邻能力域交给同一成员，也可以把高负荷能力域拆给多名成员。每个子对话保持一个半固定主责，以积累有效上下文；需要跨领域时指定一名所有者，其他成员只提供只读审查或明确分离的文件修改。

同一角色优先复用原子对话，通过后续任务继续工作，以保留领域历史。新阶段只补充自上次任务以来的变化、当前验收标准和本次文件所有权；若旧上下文已过时或明显干扰判断，才创建替代成员。

## 团队负责人工作制

- 主 agent 负责维护当前阶段的关键路径、依赖顺序、任务所有权和集成队列；不把架构取舍、跨系统修改、最终验收或阶段收口外包。
- 子 agent 采用半固定领域角色、动态任务分配。角色负责积累领域上下文，具体工作仍按阶段变化。适合委派的工作包括：独立事实核查、定向 bug 审计、测试补充、局部参数调优、素材接入、场景检查和性能采样。
- 只有任务能够独立推进、不会争抢同一文件，并且预期收益大于沟通成本时才并行。强依赖、需要频繁来回决策或几分钟可完成的修改由主 agent 直接处理。
- 每个委派任务必须包含六项：目标、允许修改的文件、禁止触碰的范围、已知事实、交付物、验证命令。没有明确文件所有权的任务只允许只读分析。
- 子 agent 只汇报增量结论：改了什么、证据是什么、风险是什么、还缺什么；不重复项目背景，不自行扩大范围，不把建议当成已决定事项。
- 主 agent 在子任务运行期间继续推进不冲突的关键路径；结果返回后先检查差异和证据，再决定采用、返工或丢弃，不能直接按报告宣布完成。
- 同一阶段只保留一个集成入口。合入顺序按“数据/接口 → 核心逻辑 → 表现与素材 → 测试与文档”排列；发现接口冲突时由主 agent统一裁决。
- 验证强度按风险分级：低风险参数或素材替换做定向检查；交互与状态修改做真实输入和回归；跨系统、存档或阶段收口才跑完整门禁。通过后没有新修改或新风险，不重复全量验证。
- 每个阶段开始时，主 agent 先向制作人给出范围、关键路径、拟并行项和出口条件；阶段结束时只提交可审阅构建、变更摘要、验证证据、已知风险和下一阶段提案，然后停下等待审阅。
- `AGENTS.md` 保存长期工作规则，`ARCHITECTURE.md` 保存阶段边界，`GDD.md` 保存已确认的玩法事实；临时任务分派和过程报告不写入长期文档。
- 换机或换团队时按 `HANDOFF.md` 完成交接；未成功推送到 `origin/main` 的本地状态不能被描述为已交付。

## 当前阶段

N7 第一章实现与自动门禁已完成。制作人已确认 UI/HUD 视觉素材，当前进入独立的 UI/HUD 完成阶段；范围和出口以 `UI_HUD_HANDOFF.md` 为准。该阶段交付并经制作人审阅前，禁止扩张到 N8 的角色美术、全局音效或学习构建。
